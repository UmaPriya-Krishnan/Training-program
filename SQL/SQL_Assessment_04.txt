umapriya.krishnan
11740611

3. SELECT * FROM EMPLOYEE WHERE SALARY > 2700 AND SALARY < 10000;

5. SELECT EMP_ID, SUBSTR(EMP_NAME, 4) AS EMP_NAME FROM EMPLOYEE;

6. SELECT EMP_ID, SUBSTR(EMP_NAME, INSTR(EMP_NAME, 'h')) AS EMP_NAME FROM EMPLOYEE;

7. select emp_id, emp_name, max(salary) as salary from  employee where salary < (select max(salary) from worker) limit1;

8. SELECT EMP_ID, EMP_NAME, SALARY FROM EMPLOYEE ORDER BY SALARY DESC LIMIT 3;

9. SELECT * FROM EMPLOYEE WHERE EMP_NAME IN (SELECT EMP_NAME FROM EMPLOYEE WHERE SALARY > 3000 AND DEPT_NAME = 'IT') 
	OR EMP_NAME IN (SELECT EMP_NAME FROM EMPLOYEE WHERE SALARY > 3400 AND DEPT_NAME = 'Admin');

10. SELECT EMP_ID, EMP_NAME, DEPT_NAME, SALARY, INSTR(EMP_NAME, 'J') AS POS FROM EMPLOYEE HAVING POS <>0;

11.SELECT EMP_ID, EMP_NAME, DEPT_NAME, SALARY FROM EMPLOYEE WHERE EMP_NAME LIKE '%J%' OR EMP_NAME LIKE '%N%' OR EMP_NAME LIKE '%M%';

12. SELECT * FROM EMPLOYEE WHERE EMP_NAME LIKE '%B%' OR EMP_NAME LIKE '%H%' AND EMP_NAME LIKE '%E%';

